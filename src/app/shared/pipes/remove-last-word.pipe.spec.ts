import { RemoveLastWordPipe } from './remove-last-word.pipe';

describe('RemoveLastWordPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveLastWordPipe();
    expect(pipe).toBeTruthy();
  });
});
